Debian-Ubuntu based distro's autoconfig for ctf's vulnboxes
#Execute debian_ubuntu_auto.sh to do everything automatically or just open zshinstall and execute the oneliner and later do the same with zshchangeconf and sshconf (remember to change USER@IP in sshconf with the actual USER and IP)
