#Fedora based distro's autoconfig for ctf's vulnboxes
#Execute fedora_auto.sh to do everything automatically or just open zshinstall and execute the oneliner and later do the same with zshchangeconf and sshconf
